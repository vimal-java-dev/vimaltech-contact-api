spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  mail:
    host: localhost
    port: 1025
    username: test
    password: test

  jpa:
    hibernate:
      ddl-auto: create-drop

  flyway:
    enabled: false

app:
  mail:
    from: test@test.com
    enabled: false
  admin:
    email: test@test.com